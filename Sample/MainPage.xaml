<Page
    x:Class="caliburn_micro_winrt_getting_started.MainPage"
    IsTabStop="false"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:caliburn_micro_winrt_getting_started"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{StaticResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button x:Name="ShowDialog" Content="Show Dialog" HorizontalAlignment="Center"/>
            <StackPanel HorizontalAlignment="Center" Orientation="Horizontal" Margin="0 20 0 0">
                <TextBlock Style="{StaticResource TitleTextStyle}" Text="Current settings value: "/>
                <TextBlock Style="{StaticResource TitleTextStyle}" x:Name="CurrentValue" Text="{Binding CurrentValue}" Margin="12 0 0 0"/>
            </StackPanel>
            <Button x:Name="ShowSettingsDialog" Content="Show Settings Dialog" HorizontalAlignment="Center" Margin="0 10 0 0" />
            <TextBlock Text="The settings dialog is available also from the settings pane" Style="{StaticResource PageSubheaderTextStyle}"/>
        </StackPanel>
    </Grid>
</Page>
